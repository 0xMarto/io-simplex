# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                                  controllers.Application.contact()
GET     /teoria                            controllers.Application.theory()
GET     /contacto                          controllers.Application.contact()
GET     /ejemplo                           controllers.Application.example()
GET     /simplex                           controllers.Application.index()
GET     /simplex/                          controllers.Application.socket()

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.at(path="/public", file)
